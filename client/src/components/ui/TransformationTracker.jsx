import React from 'react';
import { Button } from '@/components/ui/button';

const TransformationTracker = () => {
  return (
    <div className="flex justify-center">
      <Button>Transformation Tracker</Button>
    </div>
  );
};

export default TransformationTracker;
